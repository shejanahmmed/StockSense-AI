project_name: "StockSense AI"

data:
  raw_data_path: "data/raw/sample.csv"
  processed_data_path: "data/processed/"

model:
  prophet:
    growth: "linear"
    yearly_seasonality: true
    weekly_seasonality: true
    daily_seasonality: false

api:
  host: "0.0.0.0"
  port: 8000
